apiVersion: v1
kind: Service
metadata:
  name: spring-app-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      nodePort: {{ .Values.service.nodePort }} # Need to override in various environments
  selector:
    app: spring-app
